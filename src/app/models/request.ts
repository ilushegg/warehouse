export interface RequestModel {
  id: string | null,
  date: string | null,
  companyCode: string | null,
  number: string| null,
  measureCode: string | null,
  quantity: number | null,
  price: number | null,

}
